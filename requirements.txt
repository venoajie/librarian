# librarian/requirements.txt
# pydantic-settings is the successor to Pydantic's BaseSettings.

fastapi==0.110.0
uvicorn[standard]==0.27.1
chromadb==1.0.20
oci==2.123.0
redis==5.0.1
pydantic==2.8.0
pydantic-settings==2.2.1
python-dotenv==1.1.1
slowapi==0.1.9
psutil==5.9.8

# --- Service Configuration ---
LOG_LEVEL=INFO
SERVICE_VERSION="1.0.0"

# --- API Authentication ---
LIBRARIAN_API_KEY="your-super-secret-api-key"

# --- Rate Limiting ---
# Enable or disable the rate limiter entirely
RATE_LIMIT_ENABLED=true
# Example: 100 requests per 1 minute
RATE_LIMIT_TIMEFRAME="1/minute"

# --- OCI Object Storage ---
OCI_CONFIG_PATH="/home/appuser/.oci/config"
OCI_BUCKET_NAME="your-rag-index-bucket"
OCI_INDEX_OBJECT_NAME="index.tar.gz"

# --- ChromaDB ---
CHROMA_DB_PATH="/data/chroma"
CHROMA_COLLECTION_NAME="codebase_collection"

# --- Redis Cache ---
REDIS_URL="redis://redis:6379/0" # Use 'redis' hostname for Docker Compose
REDIS_CACHE_TTL_SECONDS=3600